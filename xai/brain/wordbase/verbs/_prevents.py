

from xai.brain.wordbase.verbs._prevent import _PREVENT

#calss header
class _PREVENTS(_PREVENT, ):
	def __init__(self,): 
		_PREVENT.__init__(self)
		self.name = "PREVENTS"
		self.specie = 'verbs'
		self.basic = "prevent"
		self.jsondata = {}
