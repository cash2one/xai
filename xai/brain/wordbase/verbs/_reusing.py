

from xai.brain.wordbase.verbs._reuse import _REUSE

#calss header
class _REUSING(_REUSE, ):
	def __init__(self,): 
		_REUSE.__init__(self)
		self.name = "REUSING"
		self.specie = 'verbs'
		self.basic = "reuse"
		self.jsondata = {}
