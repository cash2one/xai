

from xai.brain.wordbase.verbs._expect import _EXPECT

#calss header
class _EXPECTS(_EXPECT, ):
	def __init__(self,): 
		_EXPECT.__init__(self)
		self.name = "EXPECTS"
		self.specie = 'verbs'
		self.basic = "expect"
		self.jsondata = {}
