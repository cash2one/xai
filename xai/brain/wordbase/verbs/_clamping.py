

from xai.brain.wordbase.verbs._clamp import _CLAMP

#calss header
class _CLAMPING(_CLAMP, ):
	def __init__(self,): 
		_CLAMP.__init__(self)
		self.name = "CLAMPING"
		self.specie = 'verbs'
		self.basic = "clamp"
		self.jsondata = {}
