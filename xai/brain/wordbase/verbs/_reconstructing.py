

from xai.brain.wordbase.verbs._reconstruct import _RECONSTRUCT

#calss header
class _RECONSTRUCTING(_RECONSTRUCT, ):
	def __init__(self,): 
		_RECONSTRUCT.__init__(self)
		self.name = "RECONSTRUCTING"
		self.specie = 'verbs'
		self.basic = "reconstruct"
		self.jsondata = {}
