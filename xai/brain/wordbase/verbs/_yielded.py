

from xai.brain.wordbase.verbs._yield import _YIELD

#calss header
class _YIELDED(_YIELD, ):
	def __init__(self,): 
		_YIELD.__init__(self)
		self.name = "YIELDED"
		self.specie = 'verbs'
		self.basic = "yield"
		self.jsondata = {}
