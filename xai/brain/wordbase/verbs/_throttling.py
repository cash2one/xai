

from xai.brain.wordbase.verbs._throttle import _THROTTLE

#calss header
class _THROTTLING(_THROTTLE, ):
	def __init__(self,): 
		_THROTTLE.__init__(self)
		self.name = "THROTTLING"
		self.specie = 'verbs'
		self.basic = "throttle"
		self.jsondata = {}
