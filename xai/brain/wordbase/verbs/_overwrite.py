

#calss header
class _OVERWRITE():
	def __init__(self,): 
		self.name = "OVERWRITE"
		self.definitions = [u'If you overwrite a computer file, you replace it with a different one.', u'to write something in a way that is not clear and simple or is more detailed than it needs to be: ']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'verbs'

	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
