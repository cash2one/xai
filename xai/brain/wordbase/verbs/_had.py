

#calss header
class _HAD():
	def __init__(self,): 
		self.name = "HAD"
		self.definitions = [u'past simple and past participle of  have , also used with the past participle of other verbs to form the past perfect: ', u'(of a machine, etc.) to be in such a bad condition that it is not useful or (of a person, team, etc.) to be doing so badly that they are certain to fail: ']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'verbs'

	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
