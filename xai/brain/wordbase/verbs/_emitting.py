

from xai.brain.wordbase.verbs._emit import _EMIT

#calss header
class _EMITTING(_EMIT, ):
	def __init__(self,): 
		_EMIT.__init__(self)
		self.name = "EMITTING"
		self.specie = 'verbs'
		self.basic = "emit"
		self.jsondata = {}
