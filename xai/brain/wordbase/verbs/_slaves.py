

from xai.brain.wordbase.verbs._slave import _SLAVE

#calss header
class _SLAVES(_SLAVE, ):
	def __init__(self,): 
		_SLAVE.__init__(self)
		self.name = "SLAVES"
		self.specie = 'verbs'
		self.basic = "slave"
		self.jsondata = {}
