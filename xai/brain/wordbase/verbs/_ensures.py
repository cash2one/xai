

from xai.brain.wordbase.verbs._ensure import _ENSURE

#calss header
class _ENSURES(_ENSURE, ):
	def __init__(self,): 
		_ENSURE.__init__(self)
		self.name = "ENSURES"
		self.specie = 'verbs'
		self.basic = "ensure"
		self.jsondata = {}
