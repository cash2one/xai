

#calss header
class _STANDBY():
	def __init__(self,): 
		self.name = "STANDBY"
		self.definitions = [u'something that is always ready for use, especially if a regular one fails: ', u'When a person or a thing is on standby, they are ready to be used if necessary: ', u'a cheap ticket sold just before a flight or a performance if there is a seat available']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'nouns'


	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
