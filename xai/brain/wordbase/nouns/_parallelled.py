

from xai.brain.wordbase.nouns._parallel import _PARALLEL

#calss header
class _PARALLELLED(_PARALLEL, ):
	def __init__(self,): 
		_PARALLEL.__init__(self)
		self.name = "PARALLELLED"
		self.specie = 'nouns'
		self.basic = "parallel"
		self.jsondata = {}
