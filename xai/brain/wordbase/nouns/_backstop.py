

#calss header
class _BACKSTOP():
	def __init__(self,): 
		self.name = "BACKSTOP"
		self.definitions = [u'(in rounders) a player who stands directly behind the player from the opposing team who is trying to hit the ball, and attempts to catch the ball after it has been thrown if the person does not hit it', u'informal for  catcher ', u'(in baseball) a high fence behind the player hitting the ball, that prevents balls from leaving the playing area if they are not hit or caught']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'nouns'


	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
