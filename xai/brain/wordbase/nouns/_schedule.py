

#calss header
class _SCHEDULE():
	def __init__(self,): 
		self.name = "SCHEDULE"
		self.definitions = [u'a list of planned activities or things to be done showing the times or dates when they are intended to happen or be done: ', u'a list of the times when events are planned to happen, for example the times when classes happen or when buses, etc. leave and arrive: ', u'an official list of things: ', u'early: ', u'not early or late', u'to do less work than you planned to do by a particular point in time']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'nouns'


	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
