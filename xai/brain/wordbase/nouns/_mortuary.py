

#calss header
class _MORTUARY():
	def __init__(self,): 
		self.name = "MORTUARY"
		self.definitions = [u'a building, or a room in a hospital, etc. where dead bodies are kept before they are sent to be buried or burned, sometimes so that they can be examined', u'a place where a dead person is prepared to be buried or burned, and where relatives and friends can see the body']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'nouns'


	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
