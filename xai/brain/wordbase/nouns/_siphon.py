

#calss header
class _SIPHON():
	def __init__(self,): 
		self.name = "SIPHON"
		self.definitions = [u'a tube that is bent in the shape of an "n", with each end in a separate container at two different levels, so that liquid can be pulled up into it from the higher container and go down through it into the lower container', u'a  soda siphon ']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'nouns'


	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
