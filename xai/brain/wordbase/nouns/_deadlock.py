

#calss header
class _DEADLOCK():
	def __init__(self,): 
		self.name = "DEADLOCK"
		self.definitions = [u'a situation in which agreement in an argument cannot be reached because neither side will change its demands or accept any of the demands of the other side: ', u'a lock that is inside the edge of a door, so that it cannot be seen or removed when the door is closed']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'nouns'


	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
