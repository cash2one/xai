

#calss header
class _DECORATOR():
	def __init__(self,): 
		self.name = "DECORATOR"
		self.definitions = [u'a person whose job is to paint the inside or outside of buildings and to do other related work: ', u'a person whose job is planning how the inside of a house, etc. will look, choosing the colour of the paint, the furniture, etc.: ']

		self.parents = []
		self.childen = []
		self.properties = []
		self.jsondata = {}


		self.specie = 'nouns'


	def run(self, obj1 = [], obj2 = []):
		return self.jsondata
