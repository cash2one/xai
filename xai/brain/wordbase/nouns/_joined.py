

from xai.brain.wordbase.nouns._join import _JOIN

#calss header
class _JOINED(_JOIN, ):
	def __init__(self,): 
		_JOIN.__init__(self)
		self.name = "JOINED"
		self.specie = 'nouns'
		self.basic = "join"
		self.jsondata = {}
