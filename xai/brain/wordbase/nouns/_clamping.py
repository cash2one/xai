

from xai.brain.wordbase.nouns._clamp import _CLAMP

#calss header
class _CLAMPING(_CLAMP, ):
	def __init__(self,): 
		_CLAMP.__init__(self)
		self.name = "CLAMPING"
		self.specie = 'nouns'
		self.basic = "clamp"
		self.jsondata = {}
